---
description: Continue work saved by /handoff. Reads the HANDOFF.md note and picks up where you left off, without re-reading the whole project.
---

Continue the work a previous chat saved with /handoff. The goal is to get back to work cheaply, so read the least you can.

1. **Find the note:** HANDOFF.md in the project root. If there are several, use the most recently changed one. If none exists, tell the user "No HANDOFF.md found, nothing to resume." and stop.
2. **Read ONLY the "RESUME HERE" block** at the top first. Do not read the rest of the file yet, and do NOT scan the whole project. Reading less is the whole point.
3. **Confirm in one line** so the user knows you are oriented, for example: "Resuming: finishing the WHOOP connect button."
4. **Start working** on that next step.
5. Only read the details section below the divider, or open the listed files, if you actually need more to do the step. Pull in extra context just in time, not up front.

If the next step is blocked on something listed under "Waiting on you," ask the user that one question before you start.

$ARGUMENTS
